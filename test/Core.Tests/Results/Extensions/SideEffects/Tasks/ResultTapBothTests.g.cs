// <auto-generated>
//     This code was generated by UnambitiousFx.Core.CodeGen.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

#nullable enable

using System;
using System.Linq;
using System.Threading.Tasks;
using UnambitiousFx.Core;
using UnambitiousFx.Core.Results;
using UnambitiousFx.Core.Results.Extensions.SideEffects.Tasks;
using UnambitiousFx.Core.Results.Reasons;
using Xunit;

namespace UnambitiousFx.Core.Tests.Results.Extensions.SideEffects.Tasks;

public class ResultTapBothTaskTestsArity0
{
    #region Arity 0 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity0_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Success());
        // When
        var tappedResult = await taskResult.TapBothAsync(() => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity0_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync(() => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity0_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Success());
        // When
        var tappedResult = await taskResult.TapBothAsync(() => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity0_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync(() => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 0 - Task TapBoth
    
    #region Arity 1 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity1_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var taskResult = Task.FromResult(Result.Success(value1));
        // When
        var tappedResult = await taskResult.TapBothAsync<int>((_) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity1_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int>((_) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity1_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var taskResult = Task.FromResult(Result.Success(value1));
        // When
        var tappedResult = await taskResult.TapBothAsync<int>((_) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity1_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int>((_) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 1 - Task TapBoth
    
    #region Arity 2 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity2_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var taskResult = Task.FromResult(Result.Success(value1, value2));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string>((_, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity2_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string>((_, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity2_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var taskResult = Task.FromResult(Result.Success(value1, value2));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string>((_, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity2_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string>((_, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 2 - Task TapBoth
    
    #region Arity 3 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity3_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool>((_, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity3_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool>((_, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity3_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool>((_, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity3_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool>((_, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 3 - Task TapBoth
    
    #region Arity 4 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity4_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double>((_, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity4_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double>((_, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity4_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double>((_, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity4_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double>((_, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 4 - Task TapBoth
    
    #region Arity 5 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity5_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long>((_, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity5_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long>((_, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity5_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long>((_, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity5_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long>((_, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 5 - Task TapBoth
    
    #region Arity 6 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity6_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime>((_, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity6_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime>((_, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity6_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime>((_, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity6_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime>((_, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 6 - Task TapBoth
    
    #region Arity 7 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity7_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6, value7));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid>((_, _, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity7_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime, Guid>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid>((_, _, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity7_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6, value7));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid>((_, _, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity7_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime, Guid>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid>((_, _, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 7 - Task TapBoth
    
    #region Arity 8 - Task TapBoth
    
    [Fact]
    public async Task TapBothTask_Arity8_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var value8 = TimeSpan.FromMinutes(5);
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6, value7, value8));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid, TimeSpan>((_, _, _, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTask_Arity8_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime, Guid, TimeSpan>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid, TimeSpan>((_, _, _, _, _, _, _, _) => { successExecuted = true; return Task.CompletedTask; }, _ => { failureExecuted = true; return Task.CompletedTask; });
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity8_Success_ShouldExecuteSuccessSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var value8 = TimeSpan.FromMinutes(5);
        var taskResult = Task.FromResult(Result.Success(value1, value2, value3, value4, value5, value6, value7, value8));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid, TimeSpan>((_, _, _, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.True(successExecuted);
        Assert.False(failureExecuted);
        Assert.True(tappedResult.IsSuccess);
    }
    
    [Fact]
    public async Task TapBothTaskWithSyncActions_Arity8_Failure_ShouldExecuteFailureSideEffect() {
        // Given
        var successExecuted = false;
        var failureExecuted = false;
        var taskResult = Task.FromResult(Result.Failure<int, string, bool, double, long, DateTime, Guid, TimeSpan>("Test error"));
        // When
        var tappedResult = await taskResult.TapBothAsync<int, string, bool, double, long, DateTime, Guid, TimeSpan>((_, _, _, _, _, _, _, _) => successExecuted = true, _ => failureExecuted = true);
        // Then
        Assert.False(successExecuted);
        Assert.True(failureExecuted);
        Assert.False(tappedResult.IsSuccess);
    }
    
    #endregion // Arity 8 - Task TapBoth
}
