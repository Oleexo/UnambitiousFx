// <auto-generated>
//     This code was generated by UnambitiousFx.Core.CodeGen.
//
//     Changes to this file may cause incorrect behavior and will be lost if
//     the code is regenerated.
// </auto-generated>

#nullable enable

using System;
using System.Linq;
using System.Threading.Tasks;
using UnambitiousFx.Core;
using UnambitiousFx.Core.Results;
using UnambitiousFx.Core.Results.Extensions.ErrorHandling;
using UnambitiousFx.Core.Results.Reasons;
using Xunit;

namespace UnambitiousFx.Core.Tests.Results.Extensions.ErrorHandling;

public class ResultTryPickErrorTestsArity0
{
    #region Arity 0 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity0_Success_ShouldReturnFalse() {
        // Given
        var result = Result.Success();
        // When
        var found = result.TryPickError(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity0_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure(new Error("Test error"));
        // When
        var found = result.TryPickError(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity0_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure(new Error("Test error"));
        // When
        var found = result.TryPickError(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 0 - Sync TryPickError
    
    #region Arity 1 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity1_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var result = Result.Success(value1);
        // When
        var found = result.TryPickError<int>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity1_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int>(new Error("Test error"));
        // When
        var found = result.TryPickError<int>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity1_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int>(new Error("Test error"));
        // When
        var found = result.TryPickError<int>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 1 - Sync TryPickError
    
    #region Arity 2 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity2_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var result = Result.Success(value1, value2);
        // When
        var found = result.TryPickError<int, string>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity2_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity2_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 2 - Sync TryPickError
    
    #region Arity 3 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity3_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var result = Result.Success(value1, value2, value3);
        // When
        var found = result.TryPickError<int, string, bool>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity3_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity3_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 3 - Sync TryPickError
    
    #region Arity 4 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity4_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var result = Result.Success(value1, value2, value3, value4);
        // When
        var found = result.TryPickError<int, string, bool, double>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity4_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool, double>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity4_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool, double>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 4 - Sync TryPickError
    
    #region Arity 5 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity5_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var result = Result.Success(value1, value2, value3, value4, value5);
        // When
        var found = result.TryPickError<int, string, bool, double, long>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity5_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool, double, long>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity5_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool, double, long>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 5 - Sync TryPickError
    
    #region Arity 6 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity6_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var result = Result.Success(value1, value2, value3, value4, value5, value6);
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity6_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity6_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 6 - Sync TryPickError
    
    #region Arity 7 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity7_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var result = Result.Success(value1, value2, value3, value4, value5, value6, value7);
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity7_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime, Guid>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity7_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime, Guid>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 7 - Sync TryPickError
    
    #region Arity 8 - Sync TryPickError
    
    [Fact]
    public void TryPickError_Arity8_Success_ShouldReturnFalse() {
        // Given
        var value1 = 42;
        var value2 = "test";
        var value3 = true;
        var value4 = 3.14;
        var value5 = 123L;
        var value6 = DateTime.UtcNow;
        var value7 = Guid.NewGuid();
        var value8 = TimeSpan.FromMinutes(5);
        var result = Result.Success(value1, value2, value3, value4, value5, value6, value7, value8);
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid, TimeSpan>(e => e is Error, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    [Fact]
    public void TryPickError_Arity8_Failure_ShouldReturnFoundError() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime, Guid, TimeSpan>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid, TimeSpan>(e => e is Error, out var error);
        // Then
        Assert.True(found);
        Assert.NotNull(error);
        Assert.Equal("Test error", error.Message);
    }
    
    [Fact]
    public void TryPickError_Arity8_Failure_ShouldReturnNotFound() {
        // Given
        var result = Result.Failure<int, string, bool, double, long, DateTime, Guid, TimeSpan>(new Error("Test error"));
        // When
        var found = result.TryPickError<int, string, bool, double, long, DateTime, Guid, TimeSpan>(e => e is ExceptionalError, out var error);
        // Then
        Assert.False(found);
        Assert.Null(error);
    }
    
    #endregion // Arity 8 - Sync TryPickError
}
